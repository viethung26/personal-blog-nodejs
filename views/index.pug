html
    head
        style(type="text/css").
            a {
                text-decoration: none;
            }
            .article {
                width: 500px;
                margin: auto;
                padding: 5px;
                border-bottom: 1px solid #ddd;
            }
            .article p {
                white-space: pre-wrap;
            }
            .post-btn {
                float: right;
            }

    body
        button.post-btn
            a(href="/admin") Create new article
        each article in articles
            .article
                h2= article.title
                p
                    span 
                        i= new Date(article.createdAt).toLocaleString('vi-VN')
                    span  by 
                        b= article.author.username
                p= article.content.slice(0,150)+"... "
                    a(href="/articles/"+article.link) [read more]
